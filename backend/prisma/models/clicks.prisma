model Click {
  id        BigInt   @id @default(autoincrement())
  linkId    String
  link      Link     @relation(fields: [linkId], references: [id], onDelete: Cascade)
  Timestamp DateTime
  ipAddress String
  userAgent String

  @@map("clicks")
}
